import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppPeliculasFase1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
